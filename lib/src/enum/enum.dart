// ignore_for_file: constant_identifier_names

enum PageType { jobDetail, JobPost, leadDetail }

enum BottomSheetSource { fromClientList, frominvoice, fromjoiners }

enum JoinersTab {
  pending,
  payable,
  notpayable,
  underclause,
  otherSource,
  special,
}

enum InvoiceTab { invoice, validation, paid, reject, incorrect }

enum InvoiceButton { reject, paid, incorrct }

enum SuggestionType { jobTitle, company, resideat }

enum ClientNavBottom { clientHome, clientDetail }

enum FromWhichWorkSpace { mySpace, pipeLine, dialer }

enum TransferOrAssign { transfer, assign }
